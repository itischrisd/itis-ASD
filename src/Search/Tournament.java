package Search;

public class Tournament {

    public static int second(int[] arr, int len) {
        return kthSmallest(arr, len, 2);
    }

    private static int kthSmallest(int[] arr, int len, int k) {
        //TODO after Binary Heap
        return len;
    }
}
